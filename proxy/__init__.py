# ScopeGuard — Identity-Scoped Execution Control for AI Agents in Cloud Infrastructure
"""
ScopeGuard intercepts tool calls made by AI agents toward cloud infrastructure APIs,
scopes them against the agent's declared task, tags each call with identity metadata,
and produces a queryable audit trail for incident response.
"""

__version__ = "0.1.0"
